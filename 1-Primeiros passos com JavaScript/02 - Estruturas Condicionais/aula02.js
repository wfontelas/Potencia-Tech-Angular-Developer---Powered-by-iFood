// const numero = 1;

// const numeroPar = (numero % 2) === 0; // == - ignora o tipo da variavel, faz uma conversão implicita

// console.log(numeroPar);

const numero = 5;
const isNumeroPar = (numero % 2) === 0;

if (isNumeroPar) {
    console.log('O número é par');
}
else{
    console.log('O número é impar');
}